# Container-image scan exceptions.
#
# EMPTY BY DESIGN as of 2026-08-03. Do not add an entry here without a
# corresponding, justified entry in docs/security/dependency-exceptions.md.
#
# This previously held CVE-2026-48818 and CVE-2026-54283, the Trivy CVE aliases
# for PYSEC-2026-2281 and PYSEC-2026-249 in starlette. Both are genuinely fixed
# by the upgrade to starlette 1.3.1, so suppressing them is no longer accurate.
# A stale suppression is worse than none: it would hide a real regression if
# starlette were ever pinned back below 1.3.1.
