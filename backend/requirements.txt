# Backend runtime dependencies - fully pinned for reproducible builds.
#
# Regenerated 2026-08-03. Clears ALL 33 pip-audit findings that were open across
# 8 packages, with no remaining exceptions: `pip-audit -r` on this file reports
# "No known vulnerabilities found" with NO --ignore-vuln flags.
#
# Resolved by installing the direct dependencies below with minimum constraints
# and freezing, so transitive pins are internally consistent (pip check clean).
#
# Direct dependencies this was resolved from:
#   fastapi[standard]==0.136.0   starlette>=1.3.1   h11>=0.16.0   idna>=3.15
#   Pygments>=2.20.0   python-dotenv>=1.2.2   python-multipart>=0.0.31
#   PyJWT[crypto]>=2.13.0   click>=8.3.3   sqlalchemy[asyncio]   asyncpg
#   passlib   bcrypt==4.0.1   pyotp   openpyxl   email_validator   PyYAML
#
# Security-relevant upgrades:
#   PyJWT      2.10.1   -> 2.13.0   algorithm-policy bypass, algorithm confusion,
#                                   unsafe JWKS URL handling, token-processing
#                                   DoS. The most important item here: this
#                                   library makes every auth decision in the API.
#   starlette  0.46.1   -> 1.3.1    request smuggling, FileResponse DoS, plus the
#                                   five findings previously carried as accepted
#                                   exceptions (PYSEC-2026-161, -248, -249,
#                                   -2280, -2281) - all now actually fixed.
#   h11        0.14.0   -> 0.16.0   HTTP request smuggling
#   python-multipart 0.0.20 -> 0.0.32  traversal, form smuggling, DoS
#   idna       3.10     -> 3.18     CPU DoS
#   Pygments   2.19.1   -> 2.20.0   inefficient regex
#   python-dotenv 1.1.0 -> 1.2.2    local arbitrary-file overwrite
#   click      8.1.8    -> 8.4.2    (not in the original report; found by audit)
#   fastapi    0.115.12 -> 0.136.0  required, not optional: 0.115.x pins
#                                   starlette<0.47.0, so no starlette fix is
#                                   reachable without moving fastapi too.
#
# On starlette 1.x: fastapi 0.136.0 declares `starlette>=0.46.0` with NO upper
# bound, so the major version is permitted. Earlier notes in this repo claimed
# the five findings above were unfixable because FastAPI capped starlette below
# 1.0.0 - that was read off an older release's metadata and is wrong for
# 0.136.0. Verified: resolves clean, pip check clean, 34/34 tests pass, and
# TestClient returns identical status codes to 0.115.12/starlette 0.46.1 across
# /health, /api/v1/portal/catalog, /tests/catalog and the custom_router
# trailing-slash variant /tests/public/.
#
# fastapi is capped at 0.136.0 deliberately. From 0.137.0 onward include_router
# mounts each router as a sub-application instead of flattening routes into the
# parent. Endpoints still resolve there (checked on 0.141.1), but this app relies
# on declaration-order matching - /tests/public/ must precede /tests/{test_id} -
# so the flattened model is the one it was written against. Revisit deliberately,
# with route-ordering tests, not as a drive-by bump.
#
# bcrypt is pinned at 4.0.1 on purpose: passlib 1.7.4 reads bcrypt.__about__,
# which 4.1+ removed.
#
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
asyncpg==0.31.0
bcrypt==4.0.1
certifi==2026.7.22
cffi==2.1.0
click==8.4.2
cryptography==50.0.0
detect-installer==0.1.0
dnspython==2.8.0
email-validator==2.3.0
et_xmlfile==2.0.0
fastapi==0.136.0
fastapi-cli==0.0.32
fastapi-cloud-cli==0.23.0
fastar==0.11.0
greenlet==3.5.4
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
Jinja2==3.1.6
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
openpyxl==3.1.5
passlib==1.7.4
pycparser==3.0
pydantic==2.13.4
pydantic-extra-types==2.11.1
pydantic-settings==2.14.2
pydantic_core==2.46.4
Pygments==2.20.0
PyJWT[crypto]==2.13.0
PyOTP==2.10.0
python-dotenv==1.2.2
python-multipart==0.0.32
PyYAML==6.0.3
rich==15.0.0
rich-toolkit==0.20.3
rignore==0.8.0
sentry-sdk==2.66.1
shellingham==1.5.4
SQLAlchemy[asyncio]==2.0.51
starlette==1.3.1
typer==0.27.1
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.1
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.0.1
